{
  "name": "@devvgwardo/hermes-context",
  "version": "1.0.0",
  "description": "Hermes Context MCP — Persistent key-value context store for multi-agent coordination",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "hermes-context": "dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.build.json --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.0.0",
    "zod": "^3.24.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3"
  }
}
